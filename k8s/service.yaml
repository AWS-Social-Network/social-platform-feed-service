apiVersion: v1
kind: Service
metadata:
  name: feed-service
  namespace: default
spec:
  selector:
    app: feed-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
